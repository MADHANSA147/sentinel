# Copy this to .env and fill in your values
NEO4J_URI=neo4j+s://YOUR_AURA_INSTANCE.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password

# LLM — Groq API (OpenAI-compatible; used for MVP demo on synthetic data only)
GROQ_API_KEY=your_groq_key
GROQ_MODEL=llama-3.3-70b-versatile

# Disabled by default on small deployments. When true, the model is loaded
# once during application startup and cached for the process lifetime.
ENABLE_SEMANTIC_EMBEDDINGS=false


# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173
